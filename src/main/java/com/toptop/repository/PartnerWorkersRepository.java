package com.toptop.repository;

import com.toptop.domain.PartnerWorkers;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartnerWorkersRepository extends JpaRepository<PartnerWorkers, Long>{
}
